<div class="container">
  <div class="row">
    <div class="mx-auto mt-5">
      <h4>ようこそ、ながのCAKEへ！<br />
        このサイトは、ケーキ販売のECサイトになります。<br />
        会員でない⽅も商品の閲覧はできますが、<br />
        購⼊には会員登録が必要になります。</h4>
    </div>
  </div>


  <div class="row ml-5 mt-5">
    <h3 class="my-4 ml-5">新着商品</h3>
  </div>

  <div class="row">
    <% @items.each do |item| %>
    <div class="col-md-3 img-hidden">
       <%= link_to customer_item_path(item.id) do %>
            <%= attachment_image_tag item,:image, fallback: "no-image.png", size:"200x150", class:"attachment user profile_image fallback img-fulied" %>
        <% end %><br />
        <%= item.name %><br />
        <%= item.tax_exclude_price %>
    </div>
    <% end %>
  </div>

  <div class="row d-flex justify-content-end">
    <div>
      <%= link_to "全ての商品を見る",customer_items_path %>
    </div>
  </div>

</div>